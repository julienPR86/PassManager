#ifndef _HELP_
#define _HELP_

#include <stdio.h>
#include <string.h>
#include "utils.h"

/*Display the commands functions
if command not NULL, display the help only for the command passed in*/
int help(char *command);

#endif